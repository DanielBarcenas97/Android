<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="inter.proteco.com.soportedeidiomas">  <!--targetSdkVersion version actual de nuestro sdk -->
    <uses-sdk android:minSdkVersion="19" android:targetSdkVersion="25"></uses-sdk> <!--minSDK es la minima version -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">  <!--Quitar Action bar debemos poner -> android:theme="@style/Theme.AppCompat.NoActionBar">-->
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait">  <!--portrait es Vertical--> <!--landscape es horizontal-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>


    <!--android:icon="@mipmap/ic_launcher" icono de nuestra App -->